export enum BookType {
   FAVOURITE = "favourite",
   ON_SHELF = "onShelf",
   READ = "read"
 }
 
 export enum Name{
   APP = "MyBooks"
 }

 export enum Breakpoint{
  WIDESCREEN = 1590,
  DESKTOP = 1200,
  TABLET = 900,
  MOBILE = 768
}